# =============================================================================
# [已弃用] 旧版 docker-compose.yml - 请使用新文件名
# =============================================================================
#
# 警告：此文件已重命名！
#
# 实际配置已迁移至：docker-compose.step1-test.yml
# 此文件仅保留作为向后兼容的 wrapper。
#
# 推荐做法：
#   - CI/测试：使用 docker-compose.step1-test.yml
#   - 生产部署：使用根目录 `make deploy` 或 docker-compose.unified.yml
#
# =============================================================================

# 通过 include 引用实际配置（Docker Compose 2.20+）
include:
  - docker-compose.step1-test.yml
