# Engram 核心依赖
psycopg[binary]>=3.1.0
pyyaml>=6.0
tomli>=2.0;python_version<'3.11'

# Gateway 依赖（可选）
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
httpx>=0.24.0

# SCM 同步依赖（可选）
requests>=2.28.0
typer>=0.9.0
