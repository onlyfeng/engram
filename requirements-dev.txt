# 开发依赖
# ============================================================
# 推荐安装方式：pip install -e '.[dev]'
# 此文件保留用于兼容性，pyproject.toml [project.optional-dependencies].dev 为 SSOT
# ============================================================
-r requirements.txt

pytest>=7.0
pytest-asyncio>=0.21.0
pytest-cov
jsonschema>=4.18.0
requests-mock>=1.11.0
ruff>=0.1.0
mypy>=1.0.0

# 类型桩（Type Stubs）
# 与 pyproject.toml [project.optional-dependencies].dev 保持同步
types-requests>=2.28.0
boto3-stubs[s3]>=1.28.0
types-PyYAML>=6.0.0
types-psycopg2>=2.9.0
# tomli 无条件安装用于 mypy 检查（运行时 Python 3.11+ 使用 tomllib）
tomli>=2.0
