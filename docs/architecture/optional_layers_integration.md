# Optional Layers 接入规范

本规范定义可选层（例如 SeekDB、外部记忆服务）的接入要求、目录结构与验证语义。

---

## 1. 适用范围

- 可选组件：SeekDB、外部记忆/检索层
- 影响范围：Compose 覆盖、环境变量约束、验收流程

---

## 2. 目录与配置约束

- 可选层不应强依赖于核心组件
- 默认配置必须可在缺省环境变量下启动或显式禁用

---

## 3. 验证语义

- 必须提供健康检查入口
- 失败时应可降级到核心能力

---

更新时间：2026-01-31
